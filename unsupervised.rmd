# Load necessary libraries
library(ggplot2)
library(here)
library(dplyr)
library(readr)
library(cluster)
library(factoextra)
library(ggpubr)
library(scales)
library(caret)
library(stats)
library(skimr)
library(GGally)
library('ggcorrplot')

# Load the dataset
df <- read_csv(here("Mall_Customers.csv"))
# use this to save pics ggsave(here("images",".png"))
# View the first few rows and descriptive statistics
head(df)
summary(df)
skim_without_charts(df)
# Rename columns using snake case
colnames(df) <- c('customer_id', 'gender', 'age', 'annual_income', 'spending_score')
# Plotting distributions
columns <- c('age', 'annual_income', 'spending_score')
colnames(df)


par(mfrow = c(2, 2))
# Plotting distributions
for (col in columns) {
    hist(df[[col]], 
         main = paste("Distribution of", col), 
         xlab = col, 
         col = 'blue', 
         border = 'black', 
         breaks = 10)  # Adjust 'breaks' to control bin width
}
# Clear current plot settings


# Kernel density estimation plots with gender hue
ggplot(df, aes(x = `annual_income`, fill = gender)) + 
  geom_density(alpha = 0.5) +
  theme_minimal()

for (col in columns) {
  print(
    ggplot(df, aes(x = gender, y = .data[[col]])) + 
      geom_boxplot() +
      theme_minimal()
  )
}

# Boxplots by gender
for (col in columns) {
  print(
  ggplot(df, aes(x = gender, y = df[[col]])) + 
    geom_boxplot() +
    theme_minimal() +
    labs(title = paste("Boxplot of", col, "by gender"))
  )
}

# gender distribution
prop.table(table(df$gender))

# Scatter plot
ggplot(df, aes(x = `annual_income`, y = `spending_score`)) + 
  geom_point() +
  theme_minimal()

# Pairplot equivalent in R using GGally

ggpairs(df, aes(color = gender))

# Group by gender and summarize means
df %>%
  group_by(gender) %>%
  summarize(across(c(age, `annual_income`, `spending_score`), mean))

1# Correlation matrix and heatmap
corr_matrix <- cor(df %>% select(-gender))
ggcorrplot(corr_matrix, method = "circle", type = "lower", 
           lab = TRUE, lab_size = 3, colors = c("red", "white", "blue"))

# Bivariate clustering
set.seed(230)
kmeans2 <- kmeans(df[, c('annual_income', 'spending_score')], centers = 5)
df$SpendingIncomeCluster <- as.factor(kmeans2$cluster)
kmeans2
# Check inertia for bivariate clustering
intertia_scores2 <- sapply(1:10, function(k) {
  kmeans(df[, c('annual_income', 'spending_score')], centers = k)$tot.withinss
})

intertia_scores2

plot(1:10, intertia_scores2, type = "b", pch = 19, 
     frame = FALSE, xlab = "Number of clusters K", ylab = "Total within-clusters sum of squares")

# Cluster centers
centers <- as.data.frame(kmeans2$centers)
names(centers) <- c('x', 'y')

# Scatter plot with cluster centers
ggplot(df, aes(x = `annual_income`, y = `spending_score`, color = SpendingIncomeCluster)) + 
  geom_point(alpha = 0.7) +
  geom_point(data = centers, aes(x = x, y = y), color = "black", size = 3, shape = 8) +
  theme_minimal()

# Cross-tabulation of clusters by gender
table(df$SpendingIncomeCluster, df$gender) %>%
  prop.table(margin = 1)

# Group by cluster and calculate means
df %>%
  group_by(SpendingIncomeCluster) %>%
  summarize(across(c(age, `annual_income`, `spending_score`), mean))

# Multivariate clustering
df <- df %>%
  mutate(gender_Male = ifelse(gender == "Male", 1, 0))

# Scale the data
scale_model <- preProcess(df[, c('age', 'annual_income', 'spending_score', 'gender_Male')], method = c("center", "scale"))
scaled_data <- predict(scale_model, df[, c('age', 'annual_income', 'spending_score', 'gender_Male')])

# Fit k-means for multiple clusters
intertia_scores3 <- sapply(1:10, function(k) {
  kmeans(scaled_data, centers = k)$tot.withinss
})

plot(1:10, intertia_scores3, type = "b", pch = 19, 
     frame = FALSE, xlab = "Number of clusters K", ylab = "Total within-clusters sum of squares")

kmeans3 <- kmeans(df[, c('annual_income', 'spending_score', 'gender_Male', 'age')], centers = 6)
df$multicluster <- as.factor(kmeans3$cluster)
table(df$multicluster)

centers <- as.data.frame(kmeans3$centers)
names(centers) <- c('x', 'y')


ggplot(df, aes(x = `annual_income`, y = `spending_score`, color = multicluster)) + 
  geom_point(alpha = 0.7) +
  geom_point(data = centers, aes(x = x, y = y), color = "black", size = 3, shape = 8) +
  theme_minimal()

# Pair scatterplots with multicluster colors

ggpairs(df[,c('annual_income', 'spending_score', 'gender_Male', 'age', 'multicluster')], aes(color = multicluster), fig.show= 'hold', echo=TRUE)

df %>%
  group_by(multicluster) %>%
  summarize(
    mean_age = mean(age),
    mean_annual_income = mean(annual_income),
    mean_spending_score = mean(spending_score)
  )


  # Perform hierarchical clustering with two variables
dist_matrix <- dist(df[, c('annual_income', 'spending_score')], method = "euclidean")
hclust_model2 <- hclust(dist_matrix, method = "ward.D2")


  # Plot dendrogram
  plot(hclust_model2, hang = -1, cex = 0.6, main = "Dendrogram")

  # Cut the dendrogram into clusters
  height <- 200
  clusters <- cutree(hclust_model2, h = height)

  # Add cluster labels to the dataset
  df$HierarchicalCluster <- as.factor(clusters)

  # Scatter plot with hierarchical clusters
  ggplot(df, aes(x = `annual_income`, y = `spending_score`, color = HierarchicalCluster)) + 
    geom_point(alpha = 0.7) +
    theme_minimal()


  # Perform hierarchical clustering with all variables
  dist_matrix <- dist(df[, c('age', 'gender_Male', 'annual_income', 'spending_score')], method = "euclidean")
  hclust_model_multi <- hclust(dist_matrix, method = "ward.D2")

  # Plot dendrogram
  plot(hclust_model_multi, hang = -1, cex = 0.6, main = "Dendrogram")

  # Cut the dendrogram into clusters
  height <- 130
  clusters <- cutree(hclust_model_multi, h = height)

  # Add cluster labels to the dataset
  df$HierarchicalMultiCluster <- as.factor(clusters)

  # Scatter plot with hierarchical clusters
  ggplot(df, aes(x = `annual_income`, y = `spending_score`, color = HierarchicalMultiCluster)) + 
    geom_point(alpha = 0.7) +
    theme_minimal()






  # Cross-tabulation of clusters by mean age, spending score, and annual income
  df %>%
    group_by(multicluster) %>%
    summarize(mean_age = mean(age),
              mean_spending_score = mean(spending_score),
              mean_annual_income = mean(annual_income))

df


```{r}
kmeans2$tot.withinss
kmeans3$tot.withinss

# Assuming you have a hierarchical clustering model named hclust_model
# and you want to cut it into a specific number of clusters, e.g., 3 clusters

# Cut the dendrogram to form clusters
height <- 200
clusters <- cutree(hclust_model2, h = height)

# Function to calculate within-cluster sum of squares
calculate_withinss <- function(data, clusters) {
  total_withinss <- 0
  unique_clusters <- unique(clusters)
  
  for (cluster in unique_clusters) {
    cluster_data <- data[clusters == cluster, ]
    cluster_center <- colMeans(cluster_data)
    withinss <- sum(rowSums((cluster_data - cluster_center)^2))
    total_withinss <- total_withinss + withinss
  }
  
  return(total_withinss)
}

# Calculate the total within-cluster sum of squares for hierarchical clustering
total_withinss_hclust <- calculate_withinss(df[, c('annual_income', 'spending_score')], clusters)
total_withinss_hclust

height <- 130
clusters <- cutree(hclust_model_multi, h = height)
total_withinss_hclust_multi <- calculate_withinss(df[, c('annual_income', 'spending_score', 'age', 'gender_Male')], clusters)
total_withinss_hclust_multi
```

Numbers not making sense for hierarchical cluster models